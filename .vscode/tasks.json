{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build CUDA Debug",
            "type": "shell",
            "command": "/usr/local/cuda/bin/nvcc",
            "args": [
                "-g", "-G",
                "-o", "${workspaceFolder}/bin/main",
                "${workspaceFolder}/src/gemm/main.cu",
                "-lcublas"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": [],
            "detail": "Builds the CUDA program with debugging enabled."
        }
    ]
}
