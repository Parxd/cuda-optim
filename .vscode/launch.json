{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug CUDA GEMM",
            "type": "cuda-gdb",
            "request": "launch",
            "program": "${workspaceFolder}/build/gemm_debug",
            "args": ["-k 5"],
            "cwd": "${workspaceFolder}",
            "environment": []
        }
    ]
}