{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug CUDA GEMM",
            "type": "cuda-gdb",
            "request": "launch",
            "program": "${workspaceFolder}/bin/gemm_debug",
            "args": ["-k 4 -v 1 64 64 8"],
            "cwd": "${workspaceFolder}",
            "environment": []
        }
    ]
}